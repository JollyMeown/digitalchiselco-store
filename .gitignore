node_modules/
dist/
.astro/
.netlify/
.env
.env.local
.DS_Store
npm-debug.log*

# data working files (keep out of the app repo; they live alongside for import)
drive_token.txt
*.stl

# data artifacts — CONTAIN GOOGLE DRIVE DOWNLOAD LINKS / file IDs — never commit
*.csv
drive_stls.json
drive_folders.json
*.html

# secrets / handoff doc — NEVER commit
PROJECT_HANDOFF.md
*HANDOFF*
*SECRETS*

# scraped data artifacts
enrich.json
gap_products.json

# Local-only assets and logs (not for deploy)
BRANDING/
migration.log
migration_errors.json
migration_skipped_videos.json

# Cults3D local state (do not commit — preview may contain API key in URLs)
cults3d_uploaded.json
cults3d-upload-preview.json
